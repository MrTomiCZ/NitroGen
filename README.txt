If you dont know what to do, we have a complete video setting it up: 


1. You need to have python and in python the module requests installed, If you have not installed it, dont worry, just open InstallPython.bat to install python and InstallRequests.bat to install the module requests.
2. Change the webhook URL in the python code
If you want to execute the file just open start.bat to start it and you can do ctrl+c to stop it then close the cmd